# Only needed for the Mapbox basemap. The dashboard defaults to OpenStreetMap
# raster tiles, which need no token -- leave this blank unless you want Mapbox.
VITE_MAPBOX_TOKEN=

# Where the browser looks for the middleware.
VITE_PRAVAH_API=http://localhost:8000
